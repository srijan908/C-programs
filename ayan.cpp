#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string s;
    cin>>s;
    int len;
    cin>>len;

    int n = 0; vector<string> s;
    while(n>len-1){
        while (true){
                
        }
    }

    return 0;
}